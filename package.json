{
  "name": "editco-medical",
  "private": true,
  "scripts": {
    "dev": "npm --prefix editco_clinic run dev",
    "postinstall": "npm install --prefix editco_clinic",
    "build": "npm --prefix editco_clinic run build",
    "preview": "npm --prefix editco_clinic run preview",
    "lint": "npm --prefix editco_clinic run lint",
    "test": "npm --prefix editco_clinic run test"
  }
}
